#include<stdio.h>

	/*this is where the code begins*/

	int main(void)
{
	/*first thing is to create a  string and put it in a char container*/

	char Mystr[] = (stderr, "Programming is like building a multilingual puzzle\n");

	puts(Mystr);	/*put the string in Mystr to the screen*/

	return (0);
}
