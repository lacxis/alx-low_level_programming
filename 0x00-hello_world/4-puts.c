#include<stdio.h>
	/**this is where the code begins
	*first thing is to create a  string and put it in a char container
	*put the string in Mystr to the screen
	**/

int main(void)
{
	char Mystr[] = "\"Programming is like building a multilingual puzzle";

	puts(Mystr);

	return (0);
}
